import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {route} from './api/router.js';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const types={'.html':'text/html','.css':'text/css','.js':'text/javascript','.json':'application/json','.svg':'image/svg+xml'};
http.createServer((req,res)=>{const url=new URL(req.url,'http://localhost');if(route(req,res,url)!==false)return;let file=url.pathname==='/'?'/frontend/index.html':url.pathname; if(file==='/privacy')file='/frontend/privacy.html';if(file==='/terms')file='/frontend/terms.html';const target=path.resolve(root,'.'+file);if(!target.startsWith(root)||!fs.existsSync(target))return res.writeHead(404).end('Not found');res.writeHead(200,{'Content-Type':types[path.extname(target)]||'text/plain'});fs.createReadStream(target).pipe(res);}).listen(process.env.PORT||3000,()=>console.log('PriceFinder SA running on http://localhost:3000'));
